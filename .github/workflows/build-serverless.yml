name: Build RunPod Serverless image

# Neither DJ's machine nor the RunPod pod has Docker installed, so GitHub's
# own runners build the image instead and push it to this repo's GitHub
# Container Registry package. RunPod then just pulls the finished image --
# see tasks/todo.md "CONNECT RUNPOD SERVERLESS TO THE WEB APP".
on:
  push:
    branches: [main, "gpu-*"]
    paths:
      - Dockerfile
      - .dockerignore
      - serverless_handler.py
      - requirements.txt
      - "**.py"
  workflow_dispatch: {}

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/basketball-cv-service

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Tagged with the COMMIT as well as :latest. RunPod caches by tag, so
      # pushing a new :latest does not make a worker re-pull -- pointing the
      # serverless template at an immutable per-commit tag is what actually
      # deploys, and leaves no doubt about which code a job ran.
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
